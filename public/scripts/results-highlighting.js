/*const correctAnswers = document.getElementsByClassName("correct-answer");
const incorrectAnswers = document.getElementsByClassName("incorrect-answer");

for (const correctAnswer of correctAnswers) {
  correctAnswer.disabled = true;
  correctAnswer.style.backgroundColor = "limegreen";
  if (correctAnswer.matches("[type='checkbox']")) {
    correctAnswer.checked = true;
    correctAnswer.style.accentColor = "limegreen";
  }
}

for (const incorrectAnswer of incorrectAnswers) {
  incorrectAnswer.disabled = true;
  incorrectAnswer.style.backgroundColor = "red";
  if (incorrectAnswer.matches("[type='checkbox']")) {
    incorrectAnswer.checked = true;
    incorrectAnswer.style.accentColor = "red";
  }
}*/
